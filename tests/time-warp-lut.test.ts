import { describe, expect, it } from 'vitest'; import { VelocityEnvelope } from '../engine/director/VelocityEnvelope';
describe('Velocity-envelope semantics', () => { const warp = new VelocityEnvelope(); it('crash brakes before its exit kick', () => { expect(warp.speed('crash', 0)).toBeGreaterThan(warp.speed('crash', 0.7)); expect(warp.speed('crash', 1)).toBeGreaterThan(warp.speed('crash', 0.7)); }); it('whip remains fast entering the cut and hold slows in the middle', () => { expect(warp.speed('whip', 1)).toBeGreaterThan(warp.speed('whip', 0.5)); expect(warp.speed('hold', 0.5)).toBeLessThan(warp.speed('steady', 0.5)); }); });

import { describe, expect, it } from 'vitest'; import project from '../projects/001-demo/project.json'; import timeline from '../projects/001-demo/timeline.json'; import { PreparedSourcePlanner } from '../engine/source/PreparedSource'; import { createRenderContext } from '../engine/core/RenderContext'; import type { ProjectManifest, TimelineManifest } from '../engine/types';
describe('Source frame map', () => { it('maps every output shot frame to a bounded integer decoded-source index', () => { const manifest = new PreparedSourcePlanner().planShot(project as ProjectManifest, timeline as TimelineManifest, createRenderContext(project as ProjectManifest, 'master'), 's05', 'fingerprint', 480); expect(manifest.sourceFrameMap).toHaveLength(Math.round(0.8 * 120)); for (const frame of manifest.sourceFrameMap) expect(Number.isInteger(frame.sourceFrame) && frame.sourceFrame >= 0 && frame.sourceFrame < 480).toBe(true); }); });
